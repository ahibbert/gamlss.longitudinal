% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common_functions.R
\name{gamlss.longitudinal}
\alias{gamlss.longitudinal}
\title{Fit a longitudinal joint regression model}
\usage{
gamlss.longitudinal(
  dataset,
  margin_dist,
  copula_dist,
  time_var = NA,
  subject_var = NA,
  mu.formula = ("response ~ 1"),
  sigma.formula = ("~ 1"),
  nu.formula = ("~ 1"),
  tau.formula = ("~ 1"),
  theta.formula = ("~ 1"),
  zeta.formula = ("~ 1"),
  include_dlcopdpar = TRUE,
  inner_stop_crit = 0.1,
  outer_stop_crit = 0.1,
  start_step_size = 0.5,
  step_adjustment = 0.5,
  max_steps = 5,
  start_from = NA,
  verbose = 1,
  plot_results = FALSE,
  true_val = NA,
  method = "RS",
  max_outer_iter = 20,
  max_inner_iter = 20,
  max_negative_outer_streak = 10,
  use_backtracking = TRUE,
  backtracking_max_halves = 50,
  cg_max_stall = 5,
  cg_max_delta = 1,
  cg_max_delta_upper = NA,
  cg_armijo_c1 = 0.0001,
  cg_grad_tol = NA,
  cg_step_tol = NA,
  cg_update_lambda = FALSE,
  cg_beta_method = "none",
  cg_restart_every = NA,
  cg_lambda_update_every = 10,
  cg_wolfe = FALSE,
  cg_wolfe_c2 = 0.1,
  compute_vcov = TRUE,
  vcov_method = c("numderiv", "analytical"),
  vcov_numderiv = TRUE,
  use_Rcpp = FALSE,
  lambda_start = 5,
  lambda_penalty_K = 2
)
}
\arguments{
\item{margin_dist}{Marginal distribution specified as a gamlss family object,
e.g. GA(), NO(), PO(), NBI(), etc.}

\item{copula_dist}{Copula distribution specified as a string in VineCopula style
e.g. "t", "C" etc.}

\item{mu.formula}{Formula for the mean parameter of the marginal distribution}

\item{sigma.formula}{Formula for the sigma parameter of the marginal distribution}

\item{nu.formula}{Formula for the nu parameter of the marginal distribution}

\item{tau.formula}{Formula for the tau parameter of the marginal distribution}

\item{theta.formula}{Formula for the theta parameter of the copula distribution}

\item{zeta.formula}{Formula for the zeta parameter of the copula distribution}

\item{include_dlcopdpar}{Include the derivative of the copula likelihood with respect
to the margin parameters in the joint likelihood.}

\item{inner_stop_crit}{Stopping criterion for the inner loop}

\item{outer_stop_crit}{Stopping criterion for the outer loop}

\item{start_step_size}{Initial step size for the backfitting algorithm}

\item{step_adjustment}{Step size adjustment factor}

\item{max_steps}{Maximum number of times for reducing the step size}

\item{start_from}{Starting values for the parameters if needed}

\item{verbose}{Level of output to the console 3 = ALL, 0 = Minimal}

\item{plot_results}{Plot the results of the optimisation}

\item{true_val}{True values for the parameters if known for plotting}

\item{method}{Optimisation method to use, RS() is the default}

\item{max_outer_iter}{Maximum number of outer iterations}

\item{max_inner_iter}{Maximum number of inner iterations}

\item{max_negative_outer_streak}{Maximum number of consecutive negative outer
log-likelihood changes allowed before stopping.}

\item{cg_lambda_update_every}{Integer; for `method="CG"` only. When
`cg_update_lambda = TRUE`, update each smoother's lambda every this many
outer iterations. Use `1` to update every CG iteration.}

\item{compute_vcov}{Logical; if `TRUE` (default), compute and store the
model variance-covariance output at the end of fitting.}

\item{vcov_method}{Character; fit-time vcov method when `compute_vcov = TRUE`.
One of `"numderiv"` or `"analytical"`.}

\item{vcov_numderiv}{Logical; passed to `vcov.gamlss.longitudinal()` when
`compute_vcov = TRUE`. Retained for backwards compatibility.}

\item{use_Rcpp}{Use Rcpp for matrix operations}
}
\description{
This function fits a longitudinal model to a dataset with gamlss margins
and copula fit to dependence. Any linear or factor covariates can be fit
to any parameters of the copula or margin distributions. The model is fit
using RS() optimisation and the joint likelihood by default. Select
use dlcopdpar=FALSE to fit separately optimised models for the margin and
copula likelihoods which can be quicker with a slight loss to overall fit.
}
