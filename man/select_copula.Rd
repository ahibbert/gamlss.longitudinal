% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula_selection.R
\name{select_copula}
\alias{select_copula}
\title{Select a bivariate copula family from pseudo-observation pairs}
\usage{
select_copula(
  data = NULL,
  object = NULL,
  u1 = NULL,
  u2 = NULL,
  u = NULL,
  u_var = NULL,
  response_var = NULL,
  margin_dist = NULL,
  mu.formula = NULL,
  sigma.formula = NULL,
  nu.formula = NULL,
  tau.formula = NULL,
  subject_var = "subject",
  time_var = "time",
  families = c("N", "C", "F", "G", "J", "t"),
  lags = 1,
  criterion = c("AIC", "BIC", "logLik"),
  t_df_grid = c(3, 4, 6, 8, 12, 20, 30),
  min_pairs = 10,
  time_intercepts = FALSE
)
}
\arguments{
\item{data}{Optional long-format data frame.}

\item{object}{Optional fitted \code{gamlss.longitudinal} object.}

\item{u1, u2}{Optional vectors of paired pseudo-observations.}

\item{u}{Optional row-aligned vector of pseudo-observations for \code{data}.}

\item{u_var}{Optional name of a pseudo-observation column in \code{data}.}

\item{response_var}{Optional response column name in \code{data}. Used to create
pseudo-observations automatically when \code{u1}/\code{u2}, \code{u}, \code{u_var}, and
\code{object} are not supplied.}

\item{margin_dist}{Optional \code{gamlss.dist} family object, or a
\code{\link[=select_margin]{select_margin()}} result, used to fit the temporary marginal model for
automatic pseudo-observations. If omitted, \code{\link[=select_margin]{select_margin()}} is called and
a warning is issued.}

\item{mu.formula, sigma.formula, nu.formula, tau.formula}{Optional temporary
marginal model formulas used when \code{select_copula()} creates
pseudo-observations from \code{response_var}. If omitted, intercept-only
formulas are used unless \code{time_intercepts = TRUE} or \code{margin_dist} is a
time-intercept \code{\link[=select_margin]{select_margin()}} result.}

\item{subject_var, time_var}{Subject and time column names used when building
adjacent-time pairs from \code{data}.}

\item{families}{Candidate copula family codes. Supported values are \code{"N"},
\code{"C"}, \code{"F"}, \code{"G"}, \code{"J"}, and \code{"t"}.}

\item{lags}{Positive integer lag(s) used when forming adjacent pairs.}

\item{criterion}{Ranking criterion, one of \code{"AIC"}, \code{"BIC"}, or \code{"logLik"}.}

\item{t_df_grid}{Degrees-of-freedom grid used for the t-copula screen.}

\item{min_pairs}{Minimum number of complete pairs required.}

\item{time_intercepts}{Logical; when \code{select_copula()} creates
pseudo-observations from \code{response_var}, use time-specific intercepts in
the temporary marginal model and pass this setting through to
\code{\link[=select_margin]{select_margin()}} if the marginal family is auto-selected.}
}
\value{
A data frame with one row per family and class
\code{copula_selection}. The selected family is stored in the \code{selected}
attribute.
}
\description{
Screens supported copula families by maximising their native copula
log-likelihood on adjacent-time pseudo-observation pairs. Pseudo-observations
can be supplied directly as \code{u1}/\code{u2}, supplied as a row-aligned uniform
vector or column in \code{data}, or computed from a fitted
\code{gamlss.longitudinal} object.
}
\details{
This helper is intended as a lightweight family-screening step. It estimates
a constant dependence parameter for each candidate family; richer covariate
or smooth dependence structures should be fitted afterwards with
\code{\link[=gamlss_longitudinal]{gamlss_longitudinal()}}.
}
