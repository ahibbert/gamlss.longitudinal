% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common_functions.R
\name{plot_margin_fit}
\alias{plot_margin_fit}
\title{Plot an observed marginal response with a fitted GAMLSS density overlay}
\usage{
plot_margin_fit(
  x = NULL,
  margin_dist = NULL,
  data = NULL,
  fit = NULL,
  response_var = "response",
  bins = 30,
  grid_n = 200,
  response_scale = c("response", "log"),
  time_intercepts = FALSE,
  by_time = FALSE,
  time_var = "time",
  fit_control = gamlss::gamlss.control(n.cyc = 50),
  plot = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric response vector, data frame, or fitted
\code{gamlss.longitudinal} object.}

\item{margin_dist}{A \code{gamlss.dist} family object, family name, or
\code{margin_selection} result. Required for raw data unless \code{fit} is supplied.}

\item{data}{Optional data frame containing the response.}

\item{fit}{Optional fitted \code{gamlss.longitudinal} object. When supplied, the
fitted marginal density is overlaid using the model distribution and
row-specific fitted parameters.}

\item{response_var}{Response column name when \code{x} or \code{data} is a data frame.}

\item{bins}{Number of histogram bins.}

\item{grid_n}{Number of grid points used for the fitted density.}

\item{response_scale}{Plot the density on the original response scale or,
for positive responses, on the log-response scale.}

\item{time_intercepts}{Logical; for raw data, fit time-specific intercepts
for each marginal distribution parameter before drawing the overlay.}

\item{by_time}{Logical; facet the plot by time. For fitted longitudinal
models, fitted densities are averaged within each time point.}

\item{time_var}{Time column name used when \code{time_intercepts = TRUE} or
\code{by_time = TRUE} for raw data.}

\item{fit_control}{Control object passed to the internal \code{gamlss()} overlay
fit for raw-data plots. Defaults to \code{gamlss::gamlss.control(n.cyc = 50)}.}

\item{plot}{Logical; if TRUE, print the plot.}

\item{...}{Additional arguments reserved for future methods.}
}
\value{
Invisibly returns a list containing the plot, observed data, and
fitted density grid.
}
\description{
\code{plot_margin_fit()} replaces the common exploratory use of
\code{gamlss::histDist()} with a ggplot-based helper that also understands final
\code{gamlss.longitudinal} fits. With raw data it fits the supplied family as an
intercept-only marginal model. With a fitted longitudinal model it overlays
the average row-specific fitted marginal density.
}
