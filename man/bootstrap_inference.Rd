% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-inference-bootstrap.R
\name{bootstrap_inference}
\alias{bootstrap_inference}
\title{Bootstrap inference for fitted models}
\usage{
bootstrap_inference(
  object,
  R = 100,
  terms = NULL,
  level = 0.95,
  seed = NULL,
  fit_args = list(),
  keep_fits = FALSE,
  type = c("parametric", "cluster"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{gamlss.longitudinal} object.}

\item{R}{Number of bootstrap replicates.}

\item{terms}{Optional coefficient names, formula-term names such as
\code{"mu.treatment"}, coefficient-name prefixes, or numeric indices to
summarize.}

\item{level}{Confidence level for percentile intervals.}

\item{seed}{Optional random seed.}

\item{fit_args}{Optional named list of arguments passed to each refit, such
as \code{max_outer_iter}, \code{max_inner_iter}, or convergence tolerances.}

\item{keep_fits}{Logical; keep successful refitted model objects.}

\item{type}{Character; \code{"parametric"} simulates from the fitted copula model,
while \code{"cluster"} resamples subjects with replacement.}

\item{...}{Additional arguments passed to \code{\link[=simulate.gamlss.longitudinal]{simulate.gamlss.longitudinal()}}.}
}
\value{
An object of class \code{gamlss_longitudinal_bootstrap} with an exact
fixed-coefficient target, replicate failures, successful counts, and
adequacy status in \code{inference_contract}. Two successful replicates are a
computational minimum only, not evidence of inferential adequacy.
}
\description{
\code{bootstrap_inference()} either simulates responses from a fitted
\code{gamlss.longitudinal} model, or resamples subjects with replacement,
refits the same model to each bootstrap dataset,
and summarizes the bootstrap distribution of selected fixed coefficients.
Errors, explicit nonconvergence, and missing/non-finite target coefficients
are failed replicates; per-coefficient successful counts are reported.
It is intended for opt-in applied uncertainty checks and should be run with
enough replicates outside CRAN-time tests for final reporting.
}
