% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-optimizer-control.R
\name{gamlss_longitudinal_control}
\alias{gamlss_longitudinal_control}
\title{Optimizer controls for longitudinal GAMLSS fits}
\usage{
gamlss_longitudinal_control(
  outer_tol = NA,
  max_outer_iter = 100L,
  max_elapsed_sec = Inf,
  stop_on_convergence = TRUE,
  rs = list(),
  cg = list()
)
}
\arguments{
\item{outer_tol}{Positive outer objective-change tolerance, or \code{NA} for the
data-adaptive default.}

\item{max_outer_iter}{Maximum number of outer optimizer iterations.}

\item{max_elapsed_sec}{Maximum elapsed fitting time in seconds; \code{Inf} means
no time limit.}

\item{stop_on_convergence}{If \code{TRUE}, stop as soon as the method's convergence
contract is satisfied. If \code{FALSE}, continue to a safeguard or iteration
limit while still reporting whether the final state satisfies the contract.}

\item{rs}{Named list of RS-only controls. See Details.}

\item{cg}{Named list of CG-only controls. See Details.}
}
\value{
A \code{gamlss.longitudinal.control} object.
}
\description{
Create a validated, named control object for \code{\link[=gamlss_longitudinal]{gamlss_longitudinal()}}. Common
controls apply to both optimizers. Method-specific controls live in the \code{rs}
and \code{cg} lists so that optimizer settings cannot silently leak across routes.
}
\details{
RS controls are \code{inner_tol}, \code{max_inner_iter}, \code{max_negative_outer_streak},
\code{start_step_size}, \code{step_adjustment}, \code{max_steps}, \code{warm_start_joint},
\code{warm_start_joint_iter}, \code{use_backtracking}, \code{backtracking_max_halves},
\code{update_lambda}, \code{smooth_trust_radius}, and \code{discrete_score_method}.

CG controls are \code{max_stall}, \code{max_delta}, \code{armijo_c1}, \code{grad_tol},
\code{step_tol}, \code{update_lambda}, \code{lambda_update_every}, \code{max_lambda_updates},
\code{raw_loglik_drop_tol}, \code{line_search}, \code{max_line_search_evals},
\code{gradient_method}, \code{zeta_hessian}, \code{hessian_method}, \code{use_backtracking}, and
\code{backtracking_max_halves}. \code{grad_tol} is an optimizer stopping tolerance and
is distinct from \code{inference_control(gradient_tol = ...)}, which validates
post-fit curvature.
}
\examples{
rs_control <- gamlss_longitudinal_control(
  outer_tol = 1e-3,
  rs = list(inner_tol = 1e-4, max_inner_iter = 50)
)
cg_control <- gamlss_longitudinal_control(
  outer_tol = 1e-4,
  cg = list(grad_tol = 1e-3, step_tol = 1e-5)
)
}
