% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_distribution_selection.R
\name{select_joint_distribution}
\alias{select_joint_distribution}
\title{Select margin and copula families by joint longitudinal fit}
\usage{
select_joint_distribution(
  data,
  response_var,
  time_var = "time",
  subject_var = "subject",
  type = NULL,
  margin_families = NULL,
  copula_families = c("N", "C", "F", "G", "J", "t"),
  criterion = c("AIC", "BIC", "logLik"),
  min_pairs = 10,
  time_intercepts = FALSE,
  copula_time_intercepts = FALSE,
  try.gamlss = FALSE,
  trace = FALSE,
  progress = TRUE,
  fit_args = list(),
  keep_fits = FALSE
)
}
\arguments{
\item{data}{Long-format data frame.}

\item{response_var}{Response column name in \code{data}.}

\item{time_var, subject_var}{Time and subject identifier column names.}

\item{type}{Optional \code{gamlss::fitDist()} type passed to \code{\link[=select_margin]{select_margin()}}.}

\item{margin_families}{Optional marginal family names. When supplied, these
are used directly as the marginal candidate set; otherwise candidates come
from \code{\link[=select_margin]{select_margin()}}.}

\item{copula_families}{Candidate copula family codes. Supported values are
\code{"N"}, \code{"C"}, \code{"F"}, \code{"G"}, \code{"J"}, and \code{"t"}.}

\item{criterion}{Ranking criterion, one of \code{"AIC"}, \code{"BIC"}, or \code{"logLik"}.}

\item{min_pairs}{Minimum number of complete adjacent response pairs required
before fitting candidates.}

\item{time_intercepts}{Logical; if \code{TRUE}, pass through to
\code{\link[=select_margin]{select_margin()}} and use time-specific intercepts for each marginal
distribution parameter in the joint screening fits.}

\item{copula_time_intercepts}{Logical; if \code{TRUE}, use time-specific
intercepts for the copula dependence parameter in the joint screening fits.
Time is treated as a factor, not as a linear trend.}

\item{try.gamlss}{Passed to \code{\link[=select_margin]{select_margin()}}.}

\item{trace}{Logical; passed to \code{\link[=select_margin]{select_margin()}} and used to decide whether
candidate \code{\link[=gamlss_longitudinal]{gamlss_longitudinal()}} fit output is shown.}

\item{progress}{Logical; if \code{TRUE}, print candidate-level progress messages
with elapsed time and an estimated remaining runtime.}

\item{fit_args}{Optional named list of arguments overriding the default
\code{\link[=gamlss_longitudinal]{gamlss_longitudinal()}} screening fit settings.}

\item{keep_fits}{Logical; if \code{TRUE}, attach retained fitted models in the
\code{"fits"} attribute.}
}
\value{
A data frame with one row per margin-copula combination and class
\code{joint_distribution_selection}. Failed fits are retained with an \code{error}
message and missing fit metrics. Nonconverged fits retain their provisional
metrics and stop reason for audit, but have \code{selection_eligible = FALSE},
receive no rank, and can never be returned by \code{\link[=best_fit]{best_fit()}}.
}
\description{
\code{select_joint_distribution()} screens marginal distribution and copula
combinations by fitting intercept-only \code{\link[=gamlss_longitudinal]{gamlss_longitudinal()}} models and
ranking their joint likelihood, AIC, or BIC. Unlike \code{\link[=select_margin]{select_margin()}} and
\code{\link[=select_copula]{select_copula()}}, this selector evaluates each candidate as a full joint
longitudinal model, so it can take substantially longer on large candidate
sets.
}
