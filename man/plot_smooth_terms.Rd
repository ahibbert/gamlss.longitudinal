% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-terms.R
\name{plot_smooth_terms}
\alias{plot_smooth_terms}
\title{Plot all smooth terms with confidence bands}
\usage{
plot_smooth_terms(
  object,
  vcov_obj = NULL,
  data = NULL,
  ci_level = 0.95,
  ncol = NULL,
  ci_col = "red",
  fit_col = "black",
  ci_lty = 2,
  fit_lwd = 2,
  sort_x = TRUE,
  even_grid = TRUE,
  grid_n = 200,
  fallback_to_index = TRUE,
  setup_mfrow = TRUE,
  show_legend = TRUE
)
}
\arguments{
\item{object}{A fitted \code{gamlss.longitudinal} object.}

\item{vcov_obj}{Optional output from \code{vcov(object, ...)}. If \code{NULL}, this is
computed internally with the analytical vcov path.}

\item{data}{Optional data frame containing original covariates used for the
x-axis variable of each smooth.}

\item{ci_level}{Confidence level for pointwise intervals.}

\item{ncol}{Number of plot columns (defaults to 2 or fewer if needed).}

\item{ci_col}{Color for confidence bands.}

\item{fit_col}{Color for fitted smooth line.}

\item{ci_lty}{Line type for confidence bands.}

\item{fit_lwd}{Line width for fitted smooth.}

\item{sort_x}{Logical; sort points by x before plotting lines.}

\item{even_grid}{Logical; if TRUE, plot smooths on an evenly spaced x-grid
built over observed x-range.}

\item{grid_n}{Number of grid points when \code{even_grid = TRUE}.}

\item{fallback_to_index}{Logical; if x variable cannot be inferred, plot
against row index.}

\item{setup_mfrow}{Logical; if TRUE (default), configure par(mfrow) inside
this function. Set FALSE when caller configures layout.}

\item{show_legend}{Logical; if TRUE, draw a small legend in each panel.}
}
\value{
Invisibly returns a nested list with x, fitted values, standard
errors, and confidence limits for each smooth term.
}
\description{
This utility plots every smooth term in a fitted \code{gamlss.longitudinal} object
and computes pointwise confidence bands using the smooth coefficient
covariance matrices returned by \code{vcov.gamlss.longitudinal()}.
}
