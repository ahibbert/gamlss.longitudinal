% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common_functions.R
\name{plot_copula_fit}
\alias{plot_copula_fit}
\alias{plot_copula_overlay}
\title{Plot empirical pseudo-observation pairs with a fitted copula overlay}
\usage{
plot_copula_fit(
  data = NULL,
  copula = NULL,
  object = NULL,
  selected_fit = NULL,
  u1 = NULL,
  u2 = NULL,
  u = NULL,
  u_var = NULL,
  response_var = NULL,
  margin_dist = NULL,
  subject_var = "subject",
  time_var = "time",
  lags = 1,
  transform = c("normal", "uniform"),
  grid_n = 80,
  bins = 28,
  contour_bins = 8,
  max_pairs_overlay = 300,
  plot = TRUE,
  ...
)

plot_copula_overlay(
  data = NULL,
  copula = NULL,
  object = NULL,
  selected_fit = NULL,
  u1 = NULL,
  u2 = NULL,
  u = NULL,
  u_var = NULL,
  response_var = NULL,
  margin_dist = NULL,
  subject_var = "subject",
  time_var = "time",
  lags = 1,
  transform = c("normal", "uniform"),
  grid_n = 80,
  bins = 28,
  contour_bins = 8,
  max_pairs_overlay = 300,
  plot = TRUE,
  ...
)
}
\arguments{
\item{data}{Optional long-format data frame. If a fitted
\code{gamlss.longitudinal} object is supplied here, it is treated as \code{object}.}

\item{copula}{A \code{copula_selection} result, one-row selection data frame, or
family code. Required unless \code{object} is supplied.}

\item{object}{Optional fitted \code{gamlss.longitudinal} object.}

\item{selected_fit}{Deprecated alias for \code{copula}, useful when replacing old
vignette-local helpers.}

\item{u1, u2}{Optional direct paired pseudo-observations.}

\item{u}{Optional row-aligned pseudo-observation vector for \code{data}.}

\item{u_var}{Optional pseudo-observation column name in \code{data}.}

\item{response_var}{Optional response column name used to create temporary
pseudo-observations when \code{u}, \code{u_var}, and \code{u1}/\code{u2} are absent.}

\item{margin_dist}{Optional margin family used when creating temporary
pseudo-observations from \code{response_var}.}

\item{subject_var, time_var}{Subject and time column names.}

\item{lags}{Positive integer lag(s) used when forming pairs.}

\item{transform}{Character; either \code{"normal"} or \code{"uniform"}.}

\item{grid_n}{Grid size for fitted density contours.}

\item{bins}{Number of empirical two-dimensional bins.}

\item{contour_bins}{Number of fitted contour levels.}

\item{max_pairs_overlay}{Maximum paired observations used when averaging
fitted copula densities.}

\item{plot}{Logical; if TRUE, print the plot.}

\item{...}{Additional arguments reserved for future methods.}
}
\value{
Invisibly returns a list containing the plot, pair data, and fitted
density grid.
}
\description{
\code{plot_copula_fit()} and \code{plot_copula_overlay()} standardise the
copula-screen visual check used in examples and vignettes. They accept raw
pseudo-observations plus a \code{select_copula()} result, or a final
\code{gamlss.longitudinal} fit whose row-specific fitted copula density is
averaged over paired observations.
}
