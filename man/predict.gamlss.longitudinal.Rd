% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-predict.R
\name{predict.gamlss.longitudinal}
\alias{predict.gamlss.longitudinal}
\title{Predict from a longitudinal GAMLSS-copula model}
\usage{
\method{predict}{gamlss.longitudinal}(
  object,
  newdata = NULL,
  type = c("response", "mean", "mu", "median", "parameters", "quantile", "cdf",
    "density", "probability"),
  probs = c(0.025, 0.5, 0.975),
  q = NULL,
  y = NULL,
  direction = c("below", "above"),
  se.fit = FALSE,
  interval = c("none", "confidence"),
  level = 0.95,
  vcov_method = "analytical",
  ...
)
}
\arguments{
\item{object}{A fitted \code{gamlss.longitudinal} object.}

\item{newdata}{Optional new data. If omitted, fitted rows are used.}

\item{type}{Prediction type: \code{"mean"} returns the fitted marginal response
mean, \code{"median"} returns the fitted marginal median, \code{"mu"} returns the
fitted GAMLSS \code{mu} parameter, \code{"response"} is retained as a compatibility
alias for \code{"mu"}, \code{"parameters"} returns all fitted marginal distribution
parameters, \code{"quantile"} returns fitted marginal quantiles,
\code{"cdf"}/\code{"density"} evaluate the fitted marginal CDF or density, and
\code{"probability"} returns probabilities below or above a threshold.}

\item{probs}{Quantile probabilities when \code{type = "quantile"}.}

\item{q}{Threshold value for \code{type = "cdf"} or \code{type = "probability"}.
Defaults to observed responses for \code{type = "cdf"} when available.}

\item{y}{Evaluation value for \code{type = "density"}. Defaults to observed
responses when available.}

\item{direction}{Probability direction for \code{type = "probability"}.}

\item{se.fit}{Logical; for \code{type = "response"}, \code{"mu"}, or \code{"mean"}, return
approximate delta-method standard errors for the fitted \code{mu} linear
predictor contribution. For non-\code{mu} response means this is a first-order
approximation and should be treated as exploratory. These standard errors
use only the fixed \code{mu} coefficient block, conditional on fitted smooths
and all other parameters.}

\item{interval}{Interval type. \code{"confidence"} adds response-scale confidence
limits when \code{type = "response"}, \code{"mu"}, or \code{"mean"}.}

\item{level}{Confidence level for \code{interval = "confidence"}.}

\item{vcov_method}{Variance-covariance method passed to \code{\link[=vcov.gamlss.longitudinal]{vcov.gamlss.longitudinal()}}.}

\item{...}{Additional arguments reserved for future methods.}
}
\value{
A numeric vector for \code{type = "response"}, \code{"mu"}, \code{"mean"}, or
\code{"median"} unless standard errors or intervals are requested; a data frame
otherwise.
}
\description{
Predict from a longitudinal GAMLSS-copula model
}
\details{
\code{predict()} returns marginal summaries from the fitted distribution at each
requested row. The fitted copula/dependence structure is not used to
condition a row's prediction on that subject's other observed responses.
Instead, dependence affects prediction indirectly through the coefficients
estimated by the joint copula likelihood, and through \code{se.fit}/confidence
intervals when the fixed-coefficient covariance is computed from the joint
model. The intervals omit non-\code{mu}, smooth, fixed-smooth, and
smoothing-parameter uncertainty and are not future-observation prediction
intervals. Quantiles and other distributional outputs are plug-in fitted
distribution summaries without parameter-uncertainty intervals. Use
\code{\link[=simulate.gamlss.longitudinal]{simulate.gamlss.longitudinal()}} for fitted-data trajectory simulation that
preserves the fitted copula dependence structure. With \code{newdata},
simulation is unconditional and uses the model-implied dependence evaluated
on the supplied panel.

\code{type = "response"} is a soft-deprecated compatibility alias for \code{type = "mu"} because GAMLSS \code{mu} is not the response mean for every family. New code
should use \code{type = "mean"} for response-mean estimands or \code{type = "mu"} for
the distribution parameter.
}
