% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-terms.R
\name{plot_fixed_terms}
\alias{plot_fixed_terms}
\title{Plot all fixed terms with confidence bands}
\usage{
plot_fixed_terms(
  object,
  vcov_obj = NULL,
  ci_level = 0.95,
  ncol = NULL,
  include_intercept = FALSE,
  plot_interactions = FALSE,
  ci_col = "red",
  fit_col = "black",
  ci_lty = 2,
  fit_lwd = 2,
  sort_x = TRUE,
  fallback_to_index = TRUE,
  setup_mfrow = TRUE,
  data = NULL,
  factor_pch = 16,
  factor_cex = 1.2,
  show_legend = TRUE
)
}
\arguments{
\item{object}{A fitted \code{gamlss.longitudinal} object.}

\item{vcov_obj}{Optional output from \code{vcov(object, ...)}. If \code{NULL}, this is
computed internally with the analytical vcov path.}

\item{ci_level}{Confidence level for pointwise intervals.}

\item{ncol}{Number of plot columns (defaults to 2 or fewer if needed).}

\item{include_intercept}{Logical; include intercept columns in plots.}

\item{plot_interactions}{Logical; include interaction columns in plots.}

\item{ci_col}{Color for confidence bands.}

\item{fit_col}{Color for fitted fixed-term line.}

\item{ci_lty}{Line type for confidence bands.}

\item{fit_lwd}{Line width for fitted fixed-term line.}

\item{sort_x}{Logical; sort x-values before drawing lines.}

\item{fallback_to_index}{Logical; if x has one unique value, use index on x-axis.}

\item{setup_mfrow}{Logical; if TRUE (default), configure par(mfrow) inside
this function. Set FALSE when caller configures layout.}

\item{data}{Optional data frame used to detect factor columns and show
factor levels on x-axis for categorical fixed terms. Factor terms are grouped
into one point-and-interval plot per model term and parameter.}

\item{factor_pch}{Point symbol for factor-level estimates.}

\item{factor_cex}{Point size for factor-level estimates.}

\item{show_legend}{Logical; if TRUE, draw a small legend in each panel.}
}
\value{
Invisibly returns a nested list with x, fitted values, standard
errors, and confidence limits for each fixed term.
}
\description{
This utility plots fixed-effect term contributions for a fitted
\code{gamlss.longitudinal} object using coefficient uncertainty from
\code{vcov.gamlss.longitudinal()}.
}
\details{
For each fixed-effect design-matrix column \eqn{x_j}, it plots
\eqn{x_j \hat{\beta}_j} with pointwise confidence bands
\eqn{x_j \hat{\beta}_j \pm z_{\alpha/2}\sqrt{x_j^2 \mathrm{Var}(\hat{\beta}_j)}}.
}
